import React from "react";
import "./Spinner.css"; // Import CSS file

const Spinner = () => {
  return (
  <div className="flex justify-center items-center w-full h-screen">
     <div className="spinner"></div>
     
  </div>);
  
};

export default Spinner;